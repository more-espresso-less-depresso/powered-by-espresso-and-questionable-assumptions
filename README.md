# Running the project
You run the project for the different model given by functions in the `models.py`file by either selecting to run the `all_samples.py`or `grouped_samples.py`files depending on wanting to compare input or output space of the models.

Each of the notebooks will generate statistics and visualisations for the generated activations.